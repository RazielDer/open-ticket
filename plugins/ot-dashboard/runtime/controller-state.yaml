version: 1
status: completed
updated_at: 2026-03-31
active_slice: null
next_slice: null
notes:
  - Slice 73 completed: `npm run build` succeeded, the full dashboard node suite including `roundtrip` passed, and browser/manual checks on the authenticated fixture route covered valid save, invalid save, legacy recovery, and Security/Options regressions.
  - Slice 72 completed: `globalAdmins` now uses strict quoted role-ID JSON parsing, invalid saves re-render inline without writes or success audits, and the known legacy line-split corruption shape renders as a repaired draft warning until the next successful save.
  - Final evidence is recorded in `evidence/72-global-admin-roles-json-repair-and-legacy-recovery.md` and `evidence/73-final-global-admin-roles-json-repair-verification.md`.
  - The new planning packet lives in `evidence/global-admin-roles-json-repair-planning.md` with implementation and verification slices `72-...` and `73-...`.
  - Slice 71 completed: the full node verification set passed, equivalent local browser checks covered desktop/mobile admin and viewer auth plus authenticated admin/viewer routes, and the live `/health` route still returned `200` with `{\"ok\":true}`.
  - The brand/auth cleanup chain is complete: dashboard tabs use `public/assets/eotfs-dashboard-favicon.png`, `/login` uses centered `public/assets/eotfs-login-hero.png`, viewer login remains art-free, and shared/auth surfaces no longer show visible footer or health shortcuts.
  - Slice 70 completed: `/login` now uses centered `public/assets/eotfs-login-hero.png`, the viewer login is art-free and minimal, and visible auth-page health/header/footer clutter is removed without changing OAuth behavior.
  - Slice 70 verification passed with `npm run build` plus the `app`, `home-setup`, and `viewer-routes` node test set.
  - Slices 09 through 30 are complete and verified; the density follow-up closed without needing scope expansion beyond the shared shell, locale copy, and route/layout tests inside `plugins/ot-dashboard/**`.
  - Final editor verification passed with `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, and `operational-pages` node test set.
  - Browser/manual checks confirmed the desktop/mobile tray behavior, inventory visibility, warning reachability, and matte black shell restraint across `General`, `Options`, `Panels`, and `Questions`.
  - The UI-refinement follow-up is complete: the shared shell is narrower and denser, save bars are compact, duplicate item-card kickers are removed, and stacked/mobile toolbars no longer fall into one-button-per-row stacks.
  - The add-ons inventory follow-up is complete for `/admin/plugins`: the page is grouped by operational state and now uses calmer, flatter scan rows instead of an equal-weight card wall.
  - Slice 34 completed with build plus `app` and `operational-pages` verification, a scoped `page-addons-inventory` shell hook, inline search-toolbar layout, and a filtered empty state.
  - Slice 35 completed with build plus `app` and `operational-pages` verification, surfaced JSON preview chips, capped tag runs, and denser card fact/action hierarchy for `/admin/plugins`.
  - Slice 36 completed with full `app`, `auth`, `home-setup`, `editor-layout`, and `operational-pages` verification plus authenticated desktop/mobile browser checks for `/dash/admin/plugins`.
  - A March 28, 2026 fixture audit still showed `/dash/admin/plugins` as a clustered wall of 18 equal-weight cards; the decluttering follow-up is using the authenticated fixture surface because `127.0.0.1:3360` was not listening from the automation environment during planning.
  - Slice 37 completed with build plus `app` and `operational-pages` verification, state-based inventory groups, and client-side filtering that hides empty groups while preserving the filtered empty state.
  - Slice 38 completed with build plus `app` and `operational-pages` verification, a flatter row-based inventory layout, trimmed visible metadata, and a compact mobile rail.
  - Slice 39 completed with final build plus the `app`, `auth`, `home-setup`, `editor-layout`, and `operational-pages` node test set, plus desktop/mobile browser checks and filtered-empty-state validation for `/dash/admin/plugins`.
  - The `/admin/transcripts` follow-up is complete: the route now uses one workspace-owned header, responsive filter and operations disclosures, and a records-first layout that no longer repeats the same runtime-unavailable story across hero, alerts, and summary stacks.
  - Slice 40 completed with `npm run build` plus `transcript-workspace` and `operational-pages` verification, removing the shell hero/summary stack, moving transcript settings/add-on links into the page header, and converting filters into the shared responsive disclosure.
  - Slice 41 completed with `npm run build` plus `transcript-workspace` and `operational-pages` verification, trimming empty-state repetition, compacting the records table into transcript/archive/access columns, and tightening the runtime-unavailable mobile header from about `898px` to `661px`.
  - Slice 42 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; desktop/mobile browser checks on `/dash/admin/transcripts` confirmed matte shell styling, desktop-open/mobile-collapsed disclosures, and a mobile records start around `1453px` instead of the earlier `4786px` baseline from planning.
  - A March 29, 2026 ready-state transcript fixture audit still showed the page reading as a clustered stack because the transcript header, filtered summary, and bulk-actions surfaces all still render as separate summary slabs ahead of the records table.
  - The active transcript decluttering follow-up is focused on transcript-specific hierarchy only: tighter header facts, denser filtered summary, a compressed records toolbar, and calmer operations balance.
  - Slice 43 completed with `npm run build` plus `transcript-workspace` and `operational-pages` verification, replacing the transcript header and filtered summary card walls with denser transcript-specific scan surfaces.
  - Slice 44 completed with `npm run build` plus `transcript-workspace` and `operational-pages` verification, compressing bulk actions into a records toolbar and rebalancing operations into a calmer desktop grid while preserving mobile disclosure collapse.
  - Slice 45 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; equivalent authenticated transcript fixture checks at `1440x900` and `390x844` confirmed matte shell styling, desktop-open/mobile-collapsed disclosures, and materially shorter pre-table stack measurements.
  - A second March 29, 2026 transcript critique still found clustered subsection chrome: the records summary and bulk tools were still bordered subsections inside the records shell, the closed `More filters` row still read like a raised block, and the mobile header secondary facts still spent too much height.
  - Slice 46 completed with `npm run build` plus `transcript-workspace` and `operational-pages` verification, reworking the header into one primary metric with paired secondary facts, tightening transcript copy, flattening the advanced-filter row, and moving active filters into the filter footer.
  - Slice 47 completed with `npm run build` plus `transcript-workspace` and `operational-pages` verification, folding the filtered summary into the records heading, flattening bulk tools into a divider row, calming operations surfaces, and restoring bulk select-all state sync after the toolbar restructure.
  - Slice 48 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; equivalent authenticated transcript fixture checks at `http://127.0.0.1:3373/admin/transcripts` confirmed matte shell styling, working bulk select-all interaction, and earlier table reachability around `1238px` on desktop and `2003px` on stacked/mobile.
  - A follow-up wide-desktop critique found `/admin/plugins` and `/admin/transcripts` still leaving large unused gutters because the pages each imposed a route-specific content cap inside the admin shell.
  - Slice 49 completed with `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set, removing the dedicated add-ons and transcript page-width caps from the shared admin CSS.
  - Slice 50 completed with authenticated browser checks on the equivalent fixture routes at `http://127.0.0.1:3371/dash/admin/plugins` and `http://127.0.0.1:3371/dash/admin/transcripts`; at `1920x1080`, both pages expanded to about `1589px` and aligned with the same shell lane that `Home` uses instead of staying capped at about `1220px` and `1100px`.
  - A follow-up critique on 2026-03-29 identified `/admin/tickets` as an obsolete admin surface: it existed as a dedicated route, nav item, template, and locale block even though ticket summary data already lived on Home and the page no longer added useful workflow value.
  - Slice 51 completed with `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set, removing the Tickets rail item, deleting the retired template/locale surface, and converting `/admin/tickets` into a safe redirect to `/admin`.
  - Slice 52 completed with authenticated browser checks on the equivalent fixture route `http://127.0.0.1:3373`; Home showed no Tickets rail item while still exposing tracked ticket summary data, and navigating to `/admin/tickets` landed safely on `/admin`.
  - A March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/general` still found the General workspace reading more clustered than the later editor surfaces because the secondary sections and advanced disclosures all remained in one same-weight vertical stack.
  - Slice 53 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, rebalancing General around a clearer section hierarchy, denser mobile navigation, a calmer advanced disclosure grid, and a non-overlapping save row.
  - Slice 54 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/general` confirmed matte shell styling, a desktop `Logs`/`Limits` split, a non-overlapping save surface, and a mobile form start around `1025px` with the advanced-tools tray still collapsed by default.
  - A March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/options` still found the page clustered because the toolbar, summary strip, warning slab, and sticky save surface all competed before the main fields, and inactive type sections were still visible due to shared CSS overriding the `hidden` attribute.
  - Slice 55 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, adding a narrow shared `[hidden]` fix, reworking the Options inventory/top stage, grouping the ticket editor into clearer subsections, and converting the save surface into a final commit row.
  - Slice 56 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/options` confirmed matte shell styling, a desktop summary strip around `109px`, a final save surface around `114px`, correct `ticket`/`website`/`role` section visibility, and a fresh-load mobile summary strip around `269px` with the advanced-tools tray still collapsed by default.
  - A March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/panels` found the page still reading like a long utility form because identity, picker, preview, and embed all rendered as same-weight full-width slabs while the sticky save bar hovered near the top instead of closing the workflow.
  - Slice 57 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, reworking Panels around paired identity/embed and picker/preview lanes, moving the dropdown compatibility warning beside the picker, tightening copy, and converting the save surface into a final commit row.
  - Slice 58 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/panels` confirmed matte shell styling, a desktop summary strip around `127px`, a final save surface around `114px`, shared desktop builder lanes, a mobile summary strip around `287px`, and collapsed mobile advanced tools with the save row no longer appearing near the top of the workspace.
  - A March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/questions` found the page still reading like a straight utility form because usage, identity, advanced settings, and save all rendered as same-weight vertical sections while the sticky save bar hovered near the top instead of closing the workflow.
  - Slice 59 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, reworking Questions around a paired usage/identity layout, moving the active-reference warning into the usage section, tightening copy, and converting the save surface into a final commit row.
  - Slice 60 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/questions` confirmed matte shell styling, a desktop summary strip around `127px`, a final save surface around `114px`, a shared desktop usage/identity lane, a mobile summary strip around `271px`, and collapsed mobile advanced tools with the save row no longer appearing near the top of the workspace.
  - A second March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/general` still found wasted space after the first rebalance because the header repeated section count, the sidebar still carried redundant navigation copy, the connection stage kept command modes below the main fields, and the advanced/save copy still repeated visible actions.
  - Slice 61 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, replacing the redundant General header stat with live command-entry context, trimming navigation/save copy, tightening advanced summaries, and rebuilding the connection/support layout for better space use.
  - Slice 62 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/general` confirmed matte shell styling, a desktop connection section around `415px`, a desktop support band around `455px`, a desktop advanced section around `385px`, a desktop save row around `74px`, a mobile sidebar around `465px`, and collapsed mobile advanced tools with the save row no longer carrying redundant copy.
  - A second March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/options` still found redundant text and wasted space because inventory rows surfaced noisy button-emoji text, the panel-reference warning still spent its own top-stage slab, and nearly every ticket subsection opened with another helper paragraph.
  - Slice 63 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, trimming Options inventory/stage copy, moving the panel-reference warning into the dependency surface, rebalancing identity/dependency into a shared lane, and reducing the save row to a single `Save changes` heading.
  - Slice 64 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/options` confirmed matte shell styling, a desktop inventory rail around `338px`, a desktop top stage around `199px`, a desktop save row around `74px`, a fresh-load mobile summary strip around `184px`, and collapsed mobile advanced tools with the panel-reference warning no longer sitting in the top stage.
  - A second March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/panels` still found redundant text and wasted space because the inventory, top stage, primary lane, builder lane, advanced disclosure, and save row all repeated their own purpose through helper copy.
  - Slice 65 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, trimming Panels inventory/stage/section copy, collapsing the summary strip to label/value cards, tightening preview/inventory/save spacing, and reducing the save row to a single `Save changes` heading.
  - Slice 66 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/panels` confirmed matte shell styling, a desktop inventory rail around `219px`, a desktop top stage around `199px`, a desktop save row around `74px`, a fresh-load mobile summary strip around `184px`, and collapsed mobile advanced tools with materially shorter primary and builder lanes.
  - A second March 29, 2026 live critique of `http://127.0.0.1:3371/dash/visual/questions` still found redundant text and wasted space because the inventory, top stage, summary strip, usage card, and save row all repeated their own purpose through helper copy and long labels.
  - Slice 67 completed with `npm run build` plus `editor-layout` and `operational-pages` verification, trimming Questions inventory/stage/section copy, collapsing the summary strip to label/value cards, tightening usage/detail/save spacing, and reducing the save row to a single `Save changes` heading.
  - Slice 68 completed with final `npm run build` plus the `app`, `auth`, `home-setup`, `editor-layout`, `operational-pages`, and `transcript-workspace` node test set; authenticated browser checks on `http://127.0.0.1:3371/dash/visual/questions` confirmed matte shell styling, a desktop inventory rail around `312px`, a desktop top stage around `199px`, a desktop save row around `70px`, a fresh-load mobile summary strip around `184px`, and collapsed mobile advanced tools with materially shorter detail and save surfaces.
